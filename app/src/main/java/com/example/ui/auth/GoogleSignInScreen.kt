package com.example.ui.auth

import androidx.compose.runtime.Composable

/** Retired. Firebase/Google sign-in is no longer required. */
@Composable
fun GoogleSignInScreen(onSignedIn: () -> Unit) {
    onSignedIn()
}
